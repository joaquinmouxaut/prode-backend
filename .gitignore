node_modules
# Keep environment variables out of version control
.env

# Compiled output (build artifacts should not be versioned)
/dist

/generated/prisma

# Database backups (never commit production data)
*.dump
*.sql.gz
prode-prod-*.sql
